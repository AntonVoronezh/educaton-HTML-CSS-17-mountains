script(type="text/x-template")#slider-root
  .slider
    info(:work="currentWork")
    display(:work="currentWork")
    btns(
      @slide="handleSlide"
      :works="works"
      :currentIndex="currentIndex"
    )
    

script(type="text/x-template")#slider-info
  .slider__info-container
    .slider__info
      .slider__info-title
        .title.second-title {{work.title}}
        .works__item-text {{work.skills}}
        .btn-works-item 
          a(:href="work.link" target="_blank").works__item-link
            +icon('link', 'works__button-icon svg', 'ссылка на ' + key)
            | Посмотреть сайт  

script(type="text/x-template")#slider-display
  .slider__display
    img(:src="work.photo", alt="").slider__big-pic


script(type="text/x-template")#slider-btns
  .slider__buttons
    .slider__button(@click="slide('prev')")
      div.slider__button-container
        div.slider__button-img-wrap()
          img(:src="prevButtonWorks[currentIndex].photo", alt="").slider__button-pic
    .slider__button.slider__button_next(@click="slide('next')")
      div.slider__button-container
        div.slider__button-img-wrap(:key="")
          img(:src="nextButtonWorks[currentIndex].photo", alt="").slider__button-pic
